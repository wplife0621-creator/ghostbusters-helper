# 공개 빌드 저장소 설정

빌드 공유 페이지를 모든 방문자가 함께 보는 공개 목록으로 쓰려면 Supabase 무료 프로젝트를 연결합니다.

## 1. Supabase 테이블 만들기

Supabase 프로젝트를 만든 뒤 SQL Editor에서 아래 파일 2개 내용을 실행합니다.

- `supabase-builds.sql`: 빌드 공유 저장소
- `supabase-reports.sql`: 정보 제보/수정 저장소

## 2. 사이트 연결값 넣기

`config.js` 파일의 빈 값을 Supabase 프로젝트 값으로 바꿉니다.

```js
window.DUKHUBUSTERS_CONFIG = {
  supabaseUrl: "https://프로젝트아이디.supabase.co",
  supabaseAnonKey: "anon public key",
  buildTable: "builds",
  reportTable: "monster_reports",
};
```

Supabase Dashboard에서 찾는 위치:

- Project Settings
- API
- Project URL
- Project API keys의 `anon public`

## 3. 작동 방식

- 연결값이 있으면 빌드 등록 시 Supabase `builds` 테이블에 저장됩니다.
- 모든 방문자는 같은 공개 빌드 목록을 최신순으로 봅니다.
- 정보 제보/수정은 Supabase `monster_reports` 테이블의 검수 대기에 저장됩니다.
- 관리자가 승인하면 승인 데이터가 홈/정수 목록에 합쳐져 표시됩니다.
- 연결값이 비어 있거나 저장소 접속에 실패하면 기존처럼 현재 브라우저에 임시 저장합니다.
