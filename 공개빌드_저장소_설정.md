# 공개 저장소 설정

Supabase 프로젝트를 연결하면 빌드 공유, 정보 제보/수정, 공략 게시판, 방문자수를 모두 공개 저장소로 사용할 수 있습니다.

## 1. Supabase SQL 실행

SQL Editor에서 `supabase-all.sql`을 실행하면 아래 테이블과 첨부 저장소가 한 번에 생성됩니다. 이미 실행한 적이 있더라도 정수 몬스터명 수정과 공략 게시판 기능을 추가하려면 최신 파일을 다시 실행합니다.

- `builds`: 공개 빌드 목록
- `monster_reports`: 정보 제보/수정 검수 목록
- `site_visitors`: 전체 방문자수
- `daily_visitors`: 오늘 방문자수
- `guide_posts`: 공략글 목록
- `guide-media`: 공략글 이미지/동영상 첨부 저장소

개별 실행이 필요하면 아래 파일을 순서대로 실행합니다.

- `supabase-builds.sql`
- `supabase-reports.sql`
- `supabase-visitors.sql`
- `supabase-guides.sql`

## 2. 사이트 연결값

`config.js`에 Supabase URL과 공개 키를 넣습니다.

```js
window.DUKHUBUSTERS_CONFIG = {
  supabaseUrl: "https://프로젝트아이디.supabase.co",
  supabaseAnonKey: "publishable key",
  buildTable: "builds",
  reportTable: "monster_reports",
  visitorTable: "site_visitors",
  dailyVisitorTable: "daily_visitors",
  guideTable: "guide_posts",
  guideBucket: "guide-media",
  guideShareUrl: "",
};
```

카카오톡 미리보기에서 공략글 제목을 표시하려면 Supabase Edge Function `guide-share`를 배포한 뒤 `guideShareUrl`에 아래 주소를 넣습니다.

```js
guideShareUrl: "https://프로젝트아이디.supabase.co/functions/v1/guide-share",
```

## 3. 작동 방식

- 빌드 등록은 `builds`에 저장되고 모두에게 표시됩니다.
- 정보 제보/수정은 `monster_reports`에 검수 대기로 저장됩니다.
- 승인된 정수 제보는 정수 목록에 합쳐지며, 수정 모드에서는 몬스터명도 변경할 수 있습니다.
- 관리자는 승인되어 등록된 정수 제보를 삭제 처리할 수 있습니다.
- 방문자수는 하루 1회 기준으로 집계됩니다.
- 공략글은 누구나 작성, 수정, 삭제할 수 있고 이미지/동영상을 첨부할 수 있습니다.
- 저장소 연결에 실패하면 기존처럼 브라우저 임시 저장으로 돌아갑니다.
