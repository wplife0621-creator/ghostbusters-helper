# 고스트버스터즈 도우미 무료 웹사이트 만들기

## 결론

현재 폴더의 `index.html`, `styles.css`, `app.js`, `ghost-data.js`는 서버가 없어도 동작하는 정적 웹사이트입니다.
그래서 서버비는 무료 호스팅으로 해결할 수 있습니다.

도메인비까지 0원으로 가려면 개인 도메인 대신 무료 주소를 쓰면 됩니다.

- GitHub Pages: `아이디.github.io/저장소이름`
- Cloudflare Pages: `프로젝트이름.pages.dev`
- Netlify: `프로젝트이름.netlify.app`
- Vercel: `프로젝트이름.vercel.app`

`ghostbusters.kr` 같은 내 소유 도메인은 보통 연 단위 구매가 필요합니다.

## 추천 선택

가장 무난한 선택은 GitHub Pages입니다.

- 서버비 무료
- 무료 주소 제공
- 지금 만든 정적 파일 그대로 배포 가능
- 업데이트할 때 파일만 바꾸면 됨

Cloudflare Pages도 좋습니다. 무료 주소가 깔끔하고, 나중에 도메인을 연결할 때 관리가 편합니다.

## GitHub Pages 배포 순서

1. GitHub 계정을 만듭니다.
2. 새 저장소를 만듭니다. 예: `ghostbusters-helper`
3. 이 폴더의 아래 파일을 저장소에 올립니다.
   - `index.html`
   - `styles.css`
   - `app.js`
   - `ghost-data.js`
4. GitHub 저장소에서 `Settings`로 들어갑니다.
5. 왼쪽 메뉴의 `Pages`를 엽니다.
6. `Deploy from a branch`를 선택합니다.
7. Branch는 `main`, 폴더는 `/root`로 설정합니다.
8. 잠시 기다리면 무료 주소가 생깁니다.

## Cloudflare Pages 배포 순서

1. Cloudflare 계정을 만듭니다.
2. `Workers & Pages`에서 새 Pages 프로젝트를 만듭니다.
3. GitHub 저장소를 연결합니다.
4. 프레임워크는 없음 또는 None으로 둡니다.
5. 빌드 명령은 비워둡니다.
6. 출력 폴더도 비워두거나 `/`로 둡니다.
7. 배포하면 `프로젝트이름.pages.dev` 주소가 생깁니다.

## 나중에 업데이트하는 법

엑셀을 수정한 뒤 `ghost-data.js`만 다시 뽑으면 웹사이트 내용이 바뀝니다.
지금은 제가 수동으로 뽑아둔 상태라, 다음 단계에서 버튼 한 번으로 갱신하는 스크립트도 만들 수 있습니다.
